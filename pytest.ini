[pytest]
markers =
    live_network: tests that call real external TradingView network
    live_llm: tests that call real external LLM providers
    live_notion: tests that call real external Notion API
    integration: integration tests
